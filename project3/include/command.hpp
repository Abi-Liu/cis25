#ifndef COMMAND_HPP
#define COMMAND_HPP

namespace CommandNamespace {

    enum Command {
        Add = 1,
        Delete,
        SearchName,
        SearchNumber,
        Print,
        Exit,
        Unknown
    };

}

#endif // COMMAND_HPP
